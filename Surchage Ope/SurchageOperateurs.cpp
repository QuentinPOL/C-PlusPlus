#include <iostream>
#include <conio.h>
#include <stdlib.h>
#include <time.h>

#include "functions.h"

using namespace std;

int main(int argc, char ** argv)
{
	/*

	////////// TD1 Exo 1

	pile_entier Pile1;

	int random;
	srand(time(NULL));

	for (int i = 0; i < 20; i++)
	{
		random = rand() % 100;
		Pile1 < random;
	}

	Pile1.affiche();

	int n = NULL;
	Pile1 > n;
	cout << "\nLe dernier nombre de la pile est : " << n << endl;
	*/

	////////// TD1 Exo 2
	vecteur v1(5, 4), v2(5, 4);

	
	if (v1 == v2)
	{
		cout << "Vecteur egaux";
	}

	return 0;
}